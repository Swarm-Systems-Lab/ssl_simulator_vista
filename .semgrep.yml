rules: []

exclude:
  - path: tests/
  - path: site/
  - path: examples/

# Generic secret-detection rules now live in ssl_pydev's own bundled
# baseline, applied automatically by `ssl-pydev security` / `just security`.
# This file only carries project-specific config (excludes) layered on top
# of that baseline plus the p/ci registry pack.
