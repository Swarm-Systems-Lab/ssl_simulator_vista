name: CI

on:
  push:
    branches: ["**"]
    paths-ignore:
      - "**.md"
      - "docs/**"
  pull_request:
    paths-ignore:
      - "**.md"
      - "docs/**"
  workflow_dispatch:

jobs:
  ci:
    uses: Swarm-Systems-Lab/ssl_ci/.github/workflows/ci.yml@v1
